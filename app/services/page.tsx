import type { Metadata } from 'next';
import { ContactSection, SectionTitle, ServicesGrid } from '@/components/site';
export const metadata: Metadata = { title: 'Services', description: 'Flight tickets, visa assistance, hotel reservations,